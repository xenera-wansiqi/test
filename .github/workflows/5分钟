name: 每5分钟触发

on:
  schedule:
    # UTC时间，每小时的0,5,10,...55分钟
    - cron: "0,5,10,15,20,25,30,35,40,45,50,55 * * * *"

jobs:
  run_job:
    runs-on: ubuntu-latest
    steps:
      - name: 输出当前中国时间
        run: date -u -d '+8 hour' +"%Y-%m-%d %H:%M:%S"
