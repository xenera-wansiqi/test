name: 每两小时打印时间

on:
  schedule:
    - cron: "0 */2 * * *"  # 每两小时的整点运行

jobs:
  print_time:
    runs-on: ubuntu-latest
    steps:
      - name: 打印中国时间
        run: |
          echo "当前中国时间：$(TZ=Asia/Shanghai date '+%Y-%m-%d %H:%M:%S')"
